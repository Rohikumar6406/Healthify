spring:
  application:
    name: Healthify_ConfigServer
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          search-location: classpath:/config

server:
  port: 9094

